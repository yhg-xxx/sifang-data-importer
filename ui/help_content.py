"""使用说明内容 - 生成帮助窗口展示的富文本 HTML（四个段落，带锚点供目录跳转）。"""

from app.constants import ERROR_CONTACT


def build_help_html() -> str:
    """返回帮助窗口的完整 HTML 正文。"""
    return f"""<div>
<h1>四方信息源入库</h1>
<p>将 Excel 文件中 40+ 个 Sheet（每个对应一张表）的数据批量导入 SQL Server；
支持内置去重、失败表自动跳过并生成错误名单、失败 Sheet 一键补录，面向内部少量用户使用。</p>

<h2><a name="usage">一、使用流程</a></h2>
<ol>
<li><b>连接数据库</b>：启动后填写服务器、数据库、Schema（默认 dbo）、用户名、密码，
点击「测试连接」成功后方可进入主界面。配置自动保存，下次启动自动填充并尝试自动连接上次使用的库。</li>
<li><b>选择 Excel 文件</b>：点击「选择文件」选取 .xlsx 数据源，程序后台秒级读取全部 Sheet，
概览表格显示每个 Sheet 的序号、名称及最后导入时间。</li>
<li><b>勾选要处理的 Sheet</b>：默认全选。可点行首复选框单独勾选/取消；
先勾选起始行、再按住 Shift 点击结束行可批量勾选连续区间；底部「全选」一键全选或取消。</li>
<li><b>数据去重（可选，每周例行）</b>：点击「开始去重」→ 选择去重模式（默认全局去重，每周例行推荐）与判重列（默认 D 企业名称）
→ 保留录入时间(第一列)最早的行、删除同企业其余重复行（平手保留首次出现）；判重列为空或录入时间为空/无法解析的行不参与去重、原样保留；未勾选的 Sheet 原样复制。
输出「原名_已去重.xlsx」（目标已存在时自动加时间戳、永不覆盖任何已有文件），有删除时同时生成配对的
「原名_已去重__重复清单.csv」对照清单。去重成功后主窗口自动切换到去重后文件，可直接「开始导入」。</li>
<li><b>开始导入（失败自动跳过）</b>：点击「开始导入」→ 在确认框核对 Sheet→表名映射 → 逐表执行：
清空目标表 → 批量插入 → 提交事务。<b>某张表导入失败不会中断流程</b>：该表自动回滚、跳过，
继续导入后续表；结束后汇总「成功 X 张 / 失败 Y 张」并展示各表实际行数。</li>
<li><b>错误名单自动生成</b>：只要有失败表，程序自动在源文件同目录生成
「源文件名__导入错误名单_时间戳.xlsx」（永不覆盖）：「汇总」页按负责人统计错误行数，
可直接复制进群消息催办；「明细」页列出 Sheet、Excel 行号、列名、当前值与问题描述，可筛选。
把名单发给业务人员修正即可。</li>
<li><b>一键补录</b>：失败的 Sheet 会被记住（概览中显示为红色）。业务修正后重新选择定稿文件，
程序自动提示「只勾选上次失败的 Sheet」，确认后直接「开始导入」补录，已入库的表不受影响。</li>
</ol>

<h2><a name="menus">二、菜单功能</a></h2>
<ul>
<li><b>连接管理</b>（快捷键 Ctrl+Shift+C）：管理多条数据库连接。左侧列表点选已保存连接即回填右侧表单，
可新增 / 删除；右侧填好后「测试连接」，成功后「连接」进入主界面。</li>
<li><b>Sheet名-表映射</b>（快捷键 Ctrl+N）：查看/管理 Sheet→表名映射（含最后导入时间）。
支持搜索筛选、双击单元格编辑（序号/Sheet名称/数据库表名）、选中右键删除、右上「新增」；
「替换为 + 替换全部」可在筛选结果内批量替换 Sheet名称与数据库表名。</li>
<li><b>列映射</b>：查看 Excel 11 列 → 数据库字段的对应关系与约束说明（只读）。</li>
<li><b>使用说明</b>：即本窗口。</li>
</ul>

<h2><a name="notes">三、注意事项</a></h2>
<ul>
<li>导入为<b>全量覆盖</b>（先清空目标表再插入），请确认勾选范围后再执行。</li>
<li>每表独立事务且<b>失败不中断</b>：某 Sheet 导入失败仅该表回滚，其余表照常导入；
失败的 Sheet 标红并被记住，下次选择文件时提示一键只勾选它们补录。</li>
<li>连接中断等严重错误会中止整个导入（中止前已成功的表数据保留）。</li>
<li>目标表使用 DELETE 清空（兼容无 TRUNCATE 权限的账号）；导入前请确保 SQL Server 中表已建好。</li>
<li>Excel 日期可能以多种写法读入，程序统一归一化解析，避免 SQL Server「日期时间字段溢出」。</li>
<li>去重中途取消则输出文件仅含已处理部分；输出文件被 Excel 占用时请先关闭再重试。</li>
<li>「验证数据」按钮保留为可选的离线检查（不写库），用于排查疑难；每周例行流程通常直接导入，
导入器会自动发现并汇总全部数据错误。</li>
</ul>

<h2><a name="contact">四、遇到问题</a></h2>
<p>所有错误均会弹出提示框；本工具为内部使用，如需帮助请联系：</p>
<p><b>{ERROR_CONTACT}</b></p>
</div>"""
